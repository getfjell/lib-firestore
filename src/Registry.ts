import * as Library from '@fjell/lib';

/**
 * Re-export the fjell-lib Registry type for use in fjell-lib-firestore
 * This ensures type compatibility between the libraries
 */
export type Registry = Library.Registry;
